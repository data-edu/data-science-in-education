# Walkthrough 1

This is the first walkthrough

```{r}
# synthpop test
d <- select(esm, participant_ID, cognitive_engagement, affective_engagement, behavioral_engagement)
dd <- synthpop::syn(d)
ddd <- dd$syn
ddd[10, ]
```