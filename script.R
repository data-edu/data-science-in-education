rm(list=ls(all=TRUE))

options(digits = 3)

knitr::opts_chunk$set(
    comment = "#>",
    out.width = "100%",
    fig.align = "center",
    fig.path = "/figures",
    warning = FALSE,
    dpi = 500
    )

set.seed(2020)